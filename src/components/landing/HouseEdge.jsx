import React, { useEffect } from 'react';
import HouseEdgeTextBox from './HouseEdgeTextBox';

const HouseEdge = () => {
    useEffect(() => { }, []);
    return <HouseEdgeTextBox />;
};

export default HouseEdge;
